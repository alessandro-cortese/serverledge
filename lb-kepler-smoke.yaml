etcd.address: "127.0.0.1:2379"
api.port: 8080
registry.area: ROME

lb.arch_awareness: true
lb.mode: RoundRobin
lb.replicas: 128
lb.refresh_interval: 10

# Kepler non viene ancora usato come reward.
mab.reward.mode: latency
